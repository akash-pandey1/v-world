// 'use server'
import { RtcRole, RtcTokenBuilder } from 'agora-token'
// import { createClient } from '../supabase/server'

// Placeholder generateToken function without supabase
export async function generateToken(channelName: string) {
    // Implement your Agora token generation logic here if needed
    // Supabase is not used
    return null;
}